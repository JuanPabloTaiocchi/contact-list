import { Model } from "miragejs";
import { ModelDefinition } from "miragejs/-types";
import { PartnerExtended } from "src/models/PartnerExtended.model";

export const MiragePartner: ModelDefinition<PartnerExtended> = Model.extend({});